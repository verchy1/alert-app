specVersion: "v1beta"
serviceId: "orseAlert"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "orseAlert-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
